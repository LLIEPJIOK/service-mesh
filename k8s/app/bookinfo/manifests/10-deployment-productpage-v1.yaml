apiVersion: apps/v1
kind: Deployment
metadata:
  name: productpage-v1
  namespace: bookinfo
  labels:
    app: productpage
    version: v1
    app.kubernetes.io/name: productpage
    app.kubernetes.io/version: v1
    app.kubernetes.io/part-of: bookinfo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: productpage
      version: v1
  template:
    metadata:
      labels:
        app: productpage
        version: v1
        app.kubernetes.io/name: productpage
        app.kubernetes.io/version: v1
        app.kubernetes.io/part-of: bookinfo
    spec:
      serviceAccountName: productpage
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: productpage
          image: istio/examples-bookinfo-productpage-v1:1.20.3
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 9080
          env:
            - name: DETAILS_HOSTNAME
              value: details
            - name: REVIEWS_HOSTNAME
              value: reviews
            - name: GUNICORN_CMD_ARGS
              value: --workers 4 --threads 4 --timeout 60
          startupProbe:
            tcpSocket:
              port: 9080
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: 9080
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12
          livenessProbe:
            tcpSocket:
              port: 9080
            initialDelaySeconds: 45
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
